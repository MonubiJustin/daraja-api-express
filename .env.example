# Example .env — copy to `.env` and replace placeholders with real values.
# Notes:
# - Do NOT commit your real `.env` to source control.
# - Replace the dummy values below with credentials from your provider.

PORT=3000
CONSUMER_KEY=dummy_consumer_key_ABC123
CONSUMER_SECRET=dummy_consumer_secret_XYZ789
BUSINESS_SHORTCODE=174379 # default shortcode for sandbox
PASSKEY=EXAMPLE_PASSKEY_0123456789abcdef
TRANSACTION_TYPE=CustomerPayBillOnline
NGROK_AUTH_TOKEN=ngrok_dummy_authtoken_123
